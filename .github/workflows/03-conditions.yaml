name: Conditions
on: [push]
jobs:
  hello:
    runs-on: ubuntu-latest
    steps: #Stages
    - name: step1
      if: github.ref == 'refs/heads/feature-*'
      run: echo 'Hello World'
    - name: step2
      if: github.ref == 'refs/heads/main'
      run: echo 'Hello GitHub Actions from main Branch'
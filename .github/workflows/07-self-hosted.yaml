name: Self-hosted-runner
on: [push]
jobs:  # Pipeline. One workflow can have multiple pipelines
  hello:
    runs-on: self-hosted
    steps: #Stages
      - name: step1
        runs: echo "Hello World"
      - name: step2
        runs: echo "Hello Github Actions"
